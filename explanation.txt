Data Loader: The data loader is responsible for obtaining historical stock-market data from an external source and converting it into a structured Pandas DataFrame. Separating this operation into its own module makes the project modular because other components such as preprocessing, feature engineering, and model training can use the same data-loading function without duplicating code.